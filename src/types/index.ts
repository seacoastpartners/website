export interface Case {
    name: string
    color: string
    link: string
    areas: string[]
    image: string
    summary: string
    details: string
    tags: string[]
}

export interface Testimonial {
    name: string
    title: string
    quote: string
    image: string
    tags: string[]
}